<UserControl x:Class="Arb.NET.IDE.VisualStudio.Tool.UI.ArbEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vsui="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
             xmlns:ui="clr-namespace:Arb.NET.IDE.VisualStudio.Tool.UI"
             xmlns:converters="clr-namespace:Arb.NET.IDE.VisualStudio.Tool.UI.Converters">
    <UserControl.Resources>
        <converters:RelativePathConverter x:Key="RelativePathConverter" />
        <Style TargetType="DataGridRow">
            <Setter Property="Background"
                    Value="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBackgroundBrushKey}}" />
            <Setter Property="Foreground"
                    Value="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="BorderBrush"
                            Value="{DynamicResource {x:Static vsui:EnvironmentColors.SystemHighlightBrushKey}}" />
                    <Setter Property="BorderThickness" Value="0,1" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Background"
                    Value="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBackgroundBrushKey}}" />
            <Setter Property="Foreground"
                    Value="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}" />
            <Setter Property="BorderBrush"
                    Value="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBorderBrushKey}}" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background"
                            Value="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBackgroundBrushKey}}" />
                    <Setter Property="Foreground"
                            Value="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="4,2" />
            <Setter Property="Background"
                    Value="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBackgroundBrushKey}}" />
            <Setter Property="Foreground"
                    Value="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}" />
        </Style>
        <Style x:Key="GuideCodeTextBoxStyle" TargetType="TextBox">
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="TextWrapping" Value="NoWrap" />
            <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background"
                Value="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBackgroundBrushKey}}" />
            <Setter Property="Foreground"
                Value="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
        </Style>
    </UserControl.Resources>
    
    <Grid Background="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBackgroundBrushKey}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Directory selector + filter (row 1) and action buttons (row 2, wrapping) -->
        <StackPanel Grid.Row="0" Margin="6,6,6,4">
            <DockPanel>
                <Label Content="Directory:"
                       DockPanel.Dock="Left"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}" />
                <ComboBox x:Name="DirectoryCombo"
                          DockPanel.Dock="Left"
                          MinWidth="320"
                          VerticalAlignment="Center"
                          SelectionChanged="DirectoryCombo_OnSelectionChanged">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource RelativePathConverter}}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <Label Content="Filter:"
                       DockPanel.Dock="Left"
                       Margin="8,0,0,0"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}" />
                <TextBox x:Name="FilterTextBox"
                         MinWidth="160"
                         VerticalAlignment="Center"
                         TextChanged="FilterTextBox_OnTextChanged" />
            </DockPanel>
            <WrapPanel Margin="0,4,0,0">
                <Button Content="Add Locale"
                        Margin="0,0,4,2"
                        Padding="8,2"
                        VerticalAlignment="Center"
                        Click="AddLocaleButton_OnClick" />
                <Button Content="Add Key"
                        Margin="0,0,4,2"
                        Padding="8,2"
                        VerticalAlignment="Center"
                        Click="AddKeyButton_OnClick" />
                <Button Content="Remove Key"
                        Margin="0,0,4,2"
                        Padding="8,2"
                        VerticalAlignment="Center"
                        Click="RemoveKeyButton_OnClick" />
                <Button Content="Import CSV..."
                        Margin="0,0,4,2"
                        Padding="8,2"
                        VerticalAlignment="Center"
                        Click="ImportCsvButton_OnClick" />
                <Button Content="Export CSV..."
                        Margin="0,0,4,2"
                        Padding="8,2"
                        VerticalAlignment="Center"
                        Click="ExportCsvButton_OnClick" />
                <Button Content="Translate..."
                        Margin="0,0,4,2"
                        Padding="8,2"
                        VerticalAlignment="Center"
                        Click="TranslateButton_OnClick" />
                <Button Content="AI Settings..."
                        Margin="0,0,4,2"
                        Padding="8,2"
                        VerticalAlignment="Center"
                        Click="AiSettingsButton_OnClick" />
            </WrapPanel>
        </StackPanel>

        <Border x:Name="SetupGuidePanel"
            Grid.Row="1"
                Margin="8,0,8,6"
                Padding="10"
                BorderThickness="1"
                BorderBrush="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBorderBrushKey}}">
            <StackPanel>
                <TextBlock Text="Minimal setup"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}" />
                <TextBlock Margin="0,4,0,8"
                           Text="An empty l10n.yaml alone is not enough. Create these two files to get the editor into a usable state."
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0"
                            Padding="8"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBorderBrushKey}}">
                        <DockPanel LastChildFill="True">
                            <Button Content="Copy"
                                    DockPanel.Dock="Top"
                                    HorizontalAlignment="Right"
                                    Margin="0,0,0,6"
                                    Padding="8,2"
                                    Click="CopyL10nTemplateButton_OnClick" />
                            <TextBlock DockPanel.Dock="Top"
                                       Text="l10n.yaml"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,6"
                                       Foreground="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}" />
                            <TextBox x:Name="SetupL10nTextBox"
                                     Style="{StaticResource GuideCodeTextBoxStyle}" />
                        </DockPanel>
                    </Border>

                    <Border Grid.Column="2"
                            Padding="8"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBorderBrushKey}}">
                        <DockPanel LastChildFill="True">
                            <Button Content="Copy"
                                    DockPanel.Dock="Top"
                                    HorizontalAlignment="Right"
                                    Margin="0,0,0,6"
                                    Padding="8,2"
                                    Click="CopyArbTemplateButton_OnClick" />
                            <TextBlock DockPanel.Dock="Top"
                                       Text="en.arb"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,6"
                                       Foreground="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}" />
                            <TextBox x:Name="SetupArbTextBox"
                                     Style="{StaticResource GuideCodeTextBoxStyle}" />
                        </DockPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Loading overlay -->
        <TextBlock x:Name="LoadingLabel"
                   Grid.Row="2"
                   Text="Loading ARB data…"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="14"
                   Foreground="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}"
                   Visibility="Visible" />

        <!-- ARB data grid -->
        <DataGrid x:Name="ArbGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="True"
                  CanUserSortColumns="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All"
                  SelectionUnit="FullRow"
                  SelectionMode="Extended"
                  Background="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBackgroundBrushKey}}"
                  Foreground="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}"
                  RowBackground="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBackgroundBrushKey}}"
                  AlternatingRowBackground="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBackgroundBrushKey}}"
                  HorizontalGridLinesBrush="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBorderBrushKey}}"
                  VerticalGridLinesBrush="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBorderBrushKey}}"
                  Visibility="Collapsed"
                  CellEditEnding="ArbGrid_OnCellEditEnding"
                  PreviewKeyDown="ArbGrid_OnPreviewKeyDown">
            <DataGrid.ContextMenu>
                <ContextMenu Opened="GridContextMenu_OnOpened">
                    <MenuItem x:Name="RenameMenuItem" Header="Rename" Click="RenameMenuItem_OnClick" />
                    <MenuItem x:Name="RenamePlaceholderMenuItem" Header="Rename Placeholder..." Click="RenamePlaceholderMenuItem_OnClick" />
                    <MenuItem x:Name="DeleteKeyMenuItem" Header="Remove Key" Click="RemoveKeyMenuItem_OnClick" />
                    <MenuItem Header="Translate Selected" Click="TranslateSelectedMenuItem_OnClick" />
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
    </Grid>
</UserControl>