<pUi:DialogWindow x:Class="Arb.NET.IDE.VisualStudio.Tool.UI.TranslationSettingsDialog"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:pUi="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
              xmlns:vsui="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              mc:Ignorable="d"
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              d:DesignHeight="200"
              d:DesignWidth="300"
              Background="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBackgroundBrushKey}}">
    <Grid Margin="12"
          Background="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowBackgroundBrushKey}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Azure OpenAI Settings" FontWeight="SemiBold" Margin="0,0,0,10"
                   Foreground="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}" />

        <TextBlock Grid.Row="1" Text="Endpoint URL:"
                   Foreground="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}"
                   Margin="0,0,0,2" />
        <TextBox x:Name="EndpointBox" Grid.Row="2" Margin="0,0,0,8" />

        <TextBlock Grid.Row="3" Text="Deployment Name:"
                   Foreground="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}"
                   Margin="0,0,0,2" />
        <TextBox x:Name="DeploymentBox" Grid.Row="4" Margin="0,0,0,8" />

        <TextBlock Grid.Row="5" Text="API Key:"
                   Foreground="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}"
                   Margin="0,0,0,2" />
        <PasswordBox x:Name="ApiKeyBox" Grid.Row="6" Margin="0,0,0,8" />

        <StackPanel Grid.Row="7" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Temperature:"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource {x:Static vsui:EnvironmentColors.ToolWindowTextBrushKey}}"
                       Margin="0,0,8,0" />
            <TextBox x:Name="TemperatureBox" Width="60" VerticalAlignment="Center" />
        </StackPanel>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save"
                    Width="75"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    Click="OkButton_OnClick" />
            <Button Content="Cancel"
                    Width="75"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</pUi:DialogWindow>