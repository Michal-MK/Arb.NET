<?xml version="1.0" encoding="UTF-8"?>

<!-- This compiles, just Rider is having a stroke because XAML support is not good. -->
<Shell x:Class="ExampleFive.AppShell"
       Title="ExampleFive"
       xmlns:exampleFive="clr-namespace:ExampleFive">
    <ShellContent Title="{Arb AppTitle}"
                  ContentTemplate="{DataTemplate exampleFive:MainPage}"
                  Route="MainPage" />
</Shell>