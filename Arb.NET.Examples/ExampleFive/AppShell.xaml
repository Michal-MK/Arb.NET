<?xml version="1.0" encoding="UTF-8"?>

<!-- This compiles, just Rider is having a stroke because XAML support is not good. -->
<Shell x:Class="ExampleFive.AppShell"
       Title="ExampleFive">
    <ShellContent Title="{Arb AppTitleTwo}"
                  ContentTemplate="{DataTemplate MainPage}"
                  Route="MainPage" />
</Shell>